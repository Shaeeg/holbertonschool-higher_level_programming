# SQL more queries

Holberton School — MySQL exercises in this directory (users, privileges, queries, etc.).

## How to run a .sql file

    cat file.sql | mysql -hlocalhost -uroot -p

Replace file.sql with the script name. Enter the MySQL root password when prompted.
